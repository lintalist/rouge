; script with some code examples to test highlighting of AutoHotkey

; directives
#UseHook

; commands
SetTitleMatchMode, 2 
MsgBox % "test with green grocers's `more text ``escape character" . " = backtic 123"

Variable= ; continuation section
(LTrim join`n
 lorem ipsum
 dolor section
)

MyArray:={ "Key":"Value"
	, "AnotherKey":"AnotherValue" }

; functions
MsgBox % InStr("this","i",,1)

; if

If (var = "Certain String")
	var:=0xb ; hex
If (var <> "Certain String")
	var:=0xAF ; hex
If (var >= 5)
	var=100`% ; (TOFIX escaped % highlighted as error)
If (var < 5)
	var:=RegExReplace(var,"imsU)(\d+)","\n$1\n")
IfEqual, 5, %variable%
	var:=45+23*(67/9)

; punctuation "dotted" removed from lexer to prevent error 
; highlighting of the . here which also messed up strings
; after the Concatenation (.)
MsgBox % "some" var "strings" var3 . "one" 123 "line" ; end of line comment

String:="cell1,cell2,cell3"
Loop, parse, String, CSV
	MsgBox % A_LoopField

Loop, 10
	{
	 ToolTip, %A_Index% ; builtin variable 
	 MsgBox % A_Index ; builtin variable
	 MsgBox %AppData% ; user variable starting with a (TOFIX)
	 MsgBox %MoreAppData% ; user variable
	}

; multiple "" chars
string=<a id="1234567890" uc="1">
RegExMatch(string,"U)""(.*)"".*""(.*)""",match)
MsgBox % Match1 " & " Match2

MsgBox This is some`nText on 2 lines %WithVariable%

; Send
Send % {lbutton}
Send {U+00FA}
Send {%A_ThisHotkey%}
Send ^k ; control-k
Send {ctrl down}k{ctrl up}
Send {altdown}k{altup}
Send {left 5}
Send {right %times%}

; hotstrings with and without leading space

::btw::by the way
  ::btw::by the way

; Hotkeys

#IfWinActive ahk_exe notepad.exe
^Numpad0::
^+Numpad0::
Lalt & Space::
Numpad0::
 Numpad0::
	Numpad0: ; label
MsgBox, 1, 4, OK, This is a test - you pressed Numpad0! '
Return

Gosub, SubRoutine
Return

/*
multiline
comments
working 100%
*/

SubRoutine:
; do something
Return
